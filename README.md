# SpriteBatchDemo

엔진 없이 Win32와 Direct3D 11만으로 만든 2D 스프라이트 렌더러입니다.
같은 장면(스프라이트 수천 개)을 **개별 드로우 콜(Naive)** 방식과 **배치(Batched)** 방식으로
실시간 전환하며, 드로우 콜 개수가 프레임 시간에 주는 영향을 **숫자로 실측**합니다.

> 앞서 만든 [MiniD3D11Renderer](https://github.com/AIMHYUK/MiniD3D11Renderer)에서
> 창 생성·디바이스·스왑체인·셰이더·상수 버퍼까지 직접 구현했고, 그 위에
> "드로우 콜을 묶으면 실제로 얼마나 빨라지는가"를 측정 가능한 형태로 만든 것이 이 프로젝트입니다.

## 무엇을 측정하는가

두 모드는 **완전히 같은 정점·인덱스 버퍼에서 똑같은 픽셀**을 그립니다.
텍스처 전환도, 버퍼 갱신도 없습니다. 딱 하나만 다릅니다 — `DrawIndexed`를 몇 번 부르는가.

| 모드 | 드로우 콜 | 방식 |
|------|-----------|------|
| **Naive**   | 스프라이트 수 (N) | 스프라이트마다 `DrawIndexed(6, ...)` 를 한 번씩 |
| **Batched** | **1** | 인덱스 N×6개를 `DrawIndexed(N*6, 0, 0)` 로 한 번에 |

그래서 두 모드의 프레임 시간 차이는 **순수하게 드로우 콜 개수의 비용**입니다.
다른 변수를 모두 없애 이 하나만 격리한 것이 이 데모의 설계 의도입니다.

## 결과 (측정 후 기입)

측정 환경: GPU ____, 1280×720, **VSync OFF (ALLOW_TEARING)**, 스프라이트 100,000개

| 모드 | 드로우 콜 | 프레임 시간 |
|------|-----------|-------------|
| Naive   | 100,000 | _____ ms |
| Batched | 1       | _____ ms |

→ 드로우 콜 100,000배 감소, 프레임 시간 __배 개선

*스프라이트 수를 500 / 2000 / 10000 / 50000 / 100000 으로 바꿔가며 측정하면,
드로우 콜 방식이 몇 개부터 갈라지는지 곡선으로 보인다.*

## 조작

| 키 | 동작 |
|----|------|
| `Space` | Naive ↔ Batched 전환 |
| `Esc` | 종료 |

모드 / 스프라이트 수 / 드로우 콜 / 프레임 시간(ms) / FPS 는 **창 제목 표시줄**에 실시간 표시됩니다.

## 환경 / 빌드

- Visual Studio 2022 (v143) / C++17 / x64
- Windows SDK (`d3d11.lib`, `dxgi.lib`, `d3dcompiler.lib`, `dxguid.lib`)
- 외부 라이브러리 없음

`SpriteBatchDemo.sln`을 VS2022로 열고 x64 / **Release** 로 빌드 후 실행합니다.
(성능 비교는 최적화가 켜진 Release 에서 해야 의미가 있습니다.)
산출물은 `build\x64\Release\` 에 생기고, 셰이더는 빌드 후 exe 옆으로 자동 복사됩니다.

> **툴셋 관련:** 이 프로젝트는 최신 MSVC 툴셋(v145)으로 설정돼 있습니다.
> "빌드 도구를 찾을 수 없습니다" 경고가 뜨면, 솔루션 우클릭 → **[솔루션 대상 변경(Retarget)]**
> 에서 설치된 툴셋(예: v143)을 고르면 그대로 빌드됩니다. 소스 코드는 동일합니다.

## 구조

```
src/
  main.cpp       진입점. 창과 렌더러를 연결하고 게임 루프를 돌린다.
  Window.*       Win32 창 하나. D3D를 모른다. 크기 변경·키 입력은 콜백으로만 알린다.
  Renderer.*     디바이스·스왑체인·스프라이트 버퍼를 소유하고, Naive/Batched로 한 프레임을 그린다.
  FrameTimer.h   QueryPerformanceCounter 기반 평균 FPS / 프레임 시간(ms) 측정.
  Common.*       COM 스마트 포인터 별칭, HRESULT 검사 매크로, exe 기준 경로 해석.
shaders/
  Sprite.vs.hlsl 픽셀 좌표 -> NDC 변환.
  Sprite.ps.hlsl 정점 색 출력. (다음 단계에서 텍스처 아틀라스 샘플로 확장)
```

## 다음 단계

1. **텍스처 아틀라스** — 스프라이트에 그림을 입히고, 아틀라스 1장으로 텍스처 전환(state change) 제거
2. **움직이는 스프라이트** — 위치를 매 프레임 바꿔 DYNAMIC 정점 버퍼 + `Map(WRITE_DISCARD)` 배칭으로
3. **인스턴싱** — 정점을 단위 사각형 하나로 되돌리고 per-instance 데이터로 대역폭 절감
4. **컬링** — 화면 밖 스프라이트를 CPU에서 걸러 드로우 대상 축소

각 단계에서 무엇을 왜 그렇게 했는지는 `Docs/작업일지/`에 남깁니다.
